fn works() {
    let spaces = "        ";
    let spaces = spaces.len();
    println!("{spaces}");
}

/* 
fn does_not_work() {
    let mut spaces = "        ";
    spaces = spaces.len();
    println!("{spaces}");
}
*/

fn main(){
    works();
}