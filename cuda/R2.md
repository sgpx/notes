id,topic,problem_description,language
1,nvcc,Compile a CUDA program that prints 'Compilation test 1' using nvcc with -arch=sm_70 flag,CUDA C++
2,nvcc,Compile a CUDA program that prints 'Compilation test 2' using nvcc with -arch=sm_70 flag,CUDA C++
3,nvcc,Compile a CUDA program that prints 'Compilation test 3' using nvcc with -arch=sm_70 flag,CUDA C++
4,nvcc,Compile a CUDA program with separate compilation: compile .cu file to .o then link,CUDA C++
5,nvcc,Compile a CUDA program with debugging symbols using nvcc -G and -lineinfo flags,CUDA C++
6,nvcc,Compile a CUDA program with optimization level O3 and observe runtime difference,CUDA C++
7,nvcc,Compile a multi-file CUDA project with a kernel in one file and main in another,CUDA C++
8,nvcc,Compile a CUDA program that uses C++11 features with -std=c++11 flag,CUDA C++
9,nvcc,Write a Makefile that compiles multiple .cu files into a single executable,CUDA C++
10,nvcc,Compile a CUDA program for multiple architectures using -gencode flags,CUDA C++
11,cudaGetDeviceCount,Query and print the number of CUDA-capable GPUs (iteration 1),CUDA C++
12,cudaGetDeviceCount,Query and print the number of CUDA-capable GPUs (iteration 2),CUDA C++
13,cudaGetDeviceCount,Query and print the number of CUDA-capable GPUs (iteration 3),CUDA C++
14,cudaGetDeviceCount,Query GPU count and print a message if zero GPUs are found,CUDA C++
15,cudaGetDeviceCount,Query GPU count and store it in a variable for later use in the program,CUDA C++
16,cudaSetDevice,Set device to GPU 0 and verify the selection by printing device properties,CUDA C++
17,cudaSetDevice,Set device to GPU 1 and verify the selection by printing device properties,CUDA C++
18,cudaSetDevice,Set device to GPU 0 and verify the selection by printing device properties,CUDA C++
19,cudaSetDevice,"Set device to GPU 0, query its name, then switch to GPU 1 if available",CUDA C++
20,cudaSetDevice,Loop through all available GPUs and print their names using cudaSetDevice,CUDA C++
21,cudaSetDevice,"Set device, check for errors, and handle invalid device index gracefully",CUDA C++
22,cudaMalloc,Allocate 1MB of GPU memory and verify the allocation was successful,CUDA C++
23,cudaMalloc,Allocate 10MB of GPU memory and verify the allocation was successful,CUDA C++
24,cudaMalloc,Allocate 100MB of GPU memory and verify the allocation was successful,CUDA C++
25,cudaMalloc,Allocate 1KB of GPU memory and verify the allocation was successful,CUDA C++
26,cudaMalloc,Allocate 1GB of GPU memory and verify the allocation was successful,CUDA C++
27,cudaMalloc,Allocate memory for an array of 1000 floats and check for null pointer,CUDA C++
28,cudaMalloc,Allocate memory for a 2D array (100x100) of integers using pitch or flat allocation,CUDA C++
29,cudaMalloc,Allocate memory for a double array and verify alignment,CUDA C++
30,cudaMalloc,Allocate multiple small buffers in a loop and track total allocated memory,CUDA C++
31,cudaMalloc,"Allocate memory, intentionally request too much, and handle the error gracefully",CUDA C++
32,cudaMalloc,Allocate memory for a struct containing multiple arrays,CUDA C++
33,cudaMalloc,Allocate pinned host memory with cudaMallocHost and compare allocation speed,CUDA C++
34,cudaMalloc,Allocate zero-copy memory and access it from both host and device,CUDA C++
35,cudaFree,Allocate 10MB of GPU memory and free it while checking for memory errors,CUDA C++
36,cudaFree,Allocate 50MB of GPU memory and free it while checking for memory errors,CUDA C++
37,cudaFree,Allocate 100MB of GPU memory and free it while checking for memory errors,CUDA C++
38,cudaFree,Allocate 1KB of GPU memory and free it while checking for memory errors,CUDA C++
39,cudaFree,Allocate 1MB of GPU memory and free it while checking for memory errors,CUDA C++
40,cudaFree,"Allocate multiple buffers, free them in reverse order, and verify no leaks",CUDA C++
41,cudaFree,"Allocate memory, free it, then try to free it again and handle the double-free error",CUDA C++
42,cudaFree,Allocate and free pinned host memory with cudaFreeHost,CUDA C++
43,cudaFree,Allocate memory inside a function and ensure proper freeing before return,CUDA C++
44,cudaMemcpy,Copy a 1D array of 100 integers from host to device and back to verify correctness,CUDA C++
45,cudaMemcpy,Copy a 1D array of 1000 integers from host to device and back to verify correctness,CUDA C++
46,cudaMemcpy,Copy a 1D array of 10000 integers from host to device and back to verify correctness,CUDA C++
47,cudaMemcpy,Copy a 1D array of 100000 integers from host to device and back to verify correctness,CUDA C++
48,cudaMemcpy,Copy a 1D array of 1000000 integers from host to device and back to verify correctness,CUDA C++
49,cudaMemcpy,Copy a 1D array of 100 floats from host to device and back to verify correctness,CUDA C++
50,cudaMemcpy,Copy a 1D array of 1000 floats from host to device and back to verify correctness,CUDA C++
51,cudaMemcpy,Copy a 1D array of 10000 floats from host to device and back to verify correctness,CUDA C++
52,cudaMemcpy,Copy a 1D array of 100 doubles from host to device and back to verify correctness,CUDA C++
53,cudaMemcpy,Copy a 1D array of 1000 doubles from host to device and back to verify correctness,CUDA C++
54,cudaMemcpy,Copy a 2D array (256x256) from host to device using cudaMemcpy2D,CUDA C++
55,cudaMemcpy,Copy a 3D array (64x64x64) from host to device using cudaMemcpy3D,CUDA C++
56,cudaMemcpy,Copy data from device to device (D2D) and verify contents are identical,CUDA C++
57,cudaMemcpy,"Copy an array host-to-device, modify it on device, copy back and verify changes",CUDA C++
58,cudaMemcpy,Use cudaMemcpy with cudaMemcpyDefault and let CUDA infer transfer direction,CUDA C++
59,cudaMemcpy,Copy a struct with mixed data types from host to device,CUDA C++
60,cudaMemcpy,Copy an array using a for-loop element-by-element on host vs cudaMemcpy and compare speed,CUDA C++
61,cudaMemcpy,"Copy an array, intentionally cause a size mismatch, and detect the error",CUDA C++
62,cudaMemcpyAsync,Use cudaMemcpyAsync to transfer 1MB of data from host to device with a stream,CUDA C++
63,cudaMemcpyAsync,Use cudaMemcpyAsync to transfer 10MB of data from host to device with a stream,CUDA C++
64,cudaMemcpyAsync,Use cudaMemcpyAsync to transfer 100MB of data from host to device with a stream,CUDA C++
65,cudaMemcpyAsync,Use cudaMemcpyAsync with pinned memory and compare throughput vs pageable memory,CUDA C++
66,cudaMemcpyAsync,Use cudaMemcpyAsync to transfer data device-to-host asynchronously,CUDA C++
67,cudaMemcpyAsync,Use cudaMemcpyAsync with multiple streams to overlap data transfers,CUDA C++
68,cudaMemcpyAsync,Use cudaMemcpyAsync and immediately synchronize the stream to verify completion,CUDA C++
69,cudaMemcpyAsync,"Allocate pinned memory, use cudaMemcpyAsync, and measure transfer time with events",CUDA C++
70,__global__ kernel,Write a simple kernel that adds 1 to every element in an array of integers and launch it,CUDA C++
71,__global__ kernel,Write a simple kernel that adds 1 to every element in an array of floats and launch it,CUDA C++
72,__global__ kernel,Write a simple kernel that multiplies by 2 to every element in an array of integers and launch it,CUDA C++
73,__global__ kernel,Write a simple kernel that multiplies by 2 to every element in an array of floats and launch it,CUDA C++
74,__global__ kernel,Write a simple kernel that subtracts 5 to every element in an array of integers and launch it,CUDA C++
75,__global__ kernel,Write a simple kernel that subtracts 5 to every element in an array of floats and launch it,CUDA C++
76,__global__ kernel,Write a simple kernel that divides by 2 to every element in an array of integers and launch it,CUDA C++
77,__global__ kernel,Write a simple kernel that divides by 2 to every element in an array of floats and launch it,CUDA C++
78,__global__ kernel,Write a simple kernel that computes square to every element in an array of integers and launch it,CUDA C++
79,__global__ kernel,Write a simple kernel that computes square to every element in an array of floats and launch it,CUDA C++
80,__global__ kernel,Write a kernel that fills an array with thread index values (variation 1),CUDA C++
81,__global__ kernel,Write a kernel that fills an array with thread index values (variation 2),CUDA C++
82,__global__ kernel,Write a kernel that fills an array with thread index values (variation 3),CUDA C++
83,__global__ kernel,Write a kernel that computes element-wise addition of two arrays,CUDA C++
84,__global__ kernel,Write a kernel that computes element-wise multiplication of two arrays,CUDA C++
85,__global__ kernel,Write a kernel that applies a threshold function to each array element,CUDA C++
86,__global__ kernel,Write a kernel that counts how many elements are above a threshold using atomics,CUDA C++
87,__global__ kernel,Write a kernel that computes the sum of two arrays and stores in a third array,CUDA C++
88,__global__ kernel,Write a kernel with a for-loop inside that processes multiple elements per thread,CUDA C++
89,__global__ kernel,Write a kernel that uses a grid-stride loop to handle arbitrary array sizes,CUDA C++
90,__global__ kernel,Write a kernel that conditionally processes elements based on thread index parity,CUDA C++
91,__global__ kernel,Write a kernel that reads from one array and writes to another with an offset,CUDA C++
92,__global__ kernel,Write a kernel that uses a template to work with multiple data types,CUDA C++
93,__global__ kernel,Write a kernel that launches with 1 block and 1 thread to print a message,CUDA C++
94,__global__ kernel,Write a kernel that launches with 1 block and N threads,CUDA C++
95,__global__ kernel,Write a kernel that launches with N blocks and 1 thread each,CUDA C++
96,__host__ and __device__,Write a utility function that works on both host and device to compute element-wise sum,CUDA C++
97,__host__ and __device__,Write a utility function that works on both host and device to compute element-wise product,CUDA C++
98,__host__ and __device__,Write a utility function that works on both host and device to compute element-wise difference,CUDA C++
99,__host__ and __device__,Write a utility function that works on both host and device to compute element-wise max,CUDA C++
100,__host__ and __device__,Write a utility function that works on both host and device to compute element-wise min,CUDA C++
101,__host__ and __device__,Write a __host__ __device__ function that computes absolute value and test on both sides,CUDA C++
102,__host__ and __device__,Write a __host__ __device__ function that clamps a value between min and max,CUDA C++
103,__host__ and __device__,Write a __host__ __device__ function that computes linear interpolation,CUDA C++
104,__host__ and __device__,Write a __host__ __device__ function and call it from a kernel and from main(),CUDA C++
105,Launch configuration,Launch a kernel with a 16x16 block grid to process a 256x256 matrix,CUDA C++
106,Launch configuration,Launch a kernel with a 32x32 block grid to process a 1024x1024 matrix,CUDA C++
107,Launch configuration,Launch a kernel with a 8x8 block grid to process a 128x128 matrix,CUDA C++
108,Launch configuration,Launch a kernel with a 16x16 block grid to process a 512x512 matrix,CUDA C++
109,Launch configuration,Launch a kernel with a 32x8 block grid to process a 256x1024 matrix,CUDA C++
110,Launch configuration,Launch a 1D kernel with appropriate block and grid dimensions for a 1D problem,CUDA C++
111,Launch configuration,Launch a 2D kernel with appropriate block and grid dimensions for a 2D problem,CUDA C++
112,Launch configuration,Launch a 3D kernel with appropriate block and grid dimensions for a 3D problem,CUDA C++
113,Launch configuration,Launch a kernel with 256 threads per block and calculate grid size dynamically,CUDA C++
114,Launch configuration,"Launch a kernel with different block sizes (64, 128, 256, 512) and compare performance",CUDA C++
115,Launch configuration,Launch a kernel with too many threads per block and observe the launch failure,CUDA C++
116,Launch configuration,Launch a kernel with zero blocks and handle the error,CUDA C++
117,Launch configuration,Launch a kernel with 1D blocks to process a 2D array using row-major indexing,CUDA C++
118,Launch configuration,Launch a kernel with 2D blocks to process a 1D array by mapping 2D indices to 1D,CUDA C++
119,Launch configuration,Launch a kernel with 3D blocks to process a 3D volume (64x64x64),CUDA C++
120,Launch configuration,Dynamically calculate optimal block size based on occupancy API,CUDA C++
121,threadIdx and blockIdx,Write a kernel that computes a unique 1D index for each thread (pattern 1),CUDA C++
122,threadIdx and blockIdx,Write a kernel that computes a unique 1D index for each thread (pattern 2),CUDA C++
123,threadIdx and blockIdx,Write a kernel that computes a unique 1D index for each thread (pattern 3),CUDA C++
124,threadIdx and blockIdx,Write a kernel that computes a unique 1D index for each thread (pattern 4),CUDA C++
125,threadIdx and blockIdx,Write a kernel that computes a unique 1D index for each thread (pattern 5),CUDA C++
126,threadIdx and blockIdx,Write a kernel that computes a unique 2D index for each thread (pattern 1),CUDA C++
127,threadIdx and blockIdx,Write a kernel that computes a unique 2D index for each thread (pattern 2),CUDA C++
128,threadIdx and blockIdx,Write a kernel that computes a unique 2D index for each thread (pattern 3),CUDA C++
129,threadIdx and blockIdx,Write a kernel that computes a unique 3D index for each thread (pattern 1),CUDA C++
130,threadIdx and blockIdx,Write a kernel that computes a unique 3D index for each thread (pattern 2),CUDA C++
131,threadIdx and blockIdx,"Write a kernel that prints threadIdx, blockIdx, and blockDim for debugging",CUDA C++
132,threadIdx and blockIdx,Write a kernel that handles boundary conditions when indexing beyond array size,CUDA C++
133,threadIdx and blockIdx,Write a kernel that maps 2D thread indices to a 1D array index,CUDA C++
134,threadIdx and blockIdx,Write a kernel that maps 1D thread indices to a 2D array index,CUDA C++
135,threadIdx and blockIdx,Write a kernel that uses blockIdx to determine which tile of data to process,CUDA C++
136,threadIdx and blockIdx,Write a kernel that computes global thread ID across all blocks and grid dimensions,CUDA C++
137,threadIdx and blockIdx,Write a kernel that uses warp ID (threadIdx.x / 32) to distribute work,CUDA C++
138,threadIdx and blockIdx,Write a kernel that checks if a thread is the first in its block and does special work,CUDA C++
139,threadIdx and blockIdx,Write a kernel that checks if a thread is the last in its block and does special work,CUDA C++
140,threadIdx and blockIdx,Write a kernel that uses lane ID (threadIdx.x % 32) for warp-level operations,CUDA C++
141,__syncthreads,Write a kernel that uses shared memory and __syncthreads to compute a block-level sum reduction (variant 1),CUDA C++
142,__syncthreads,Write a kernel that uses shared memory and __syncthreads to compute a block-level sum reduction (variant 2),CUDA C++
143,__syncthreads,Write a kernel that uses shared memory and __syncthreads to compute a block-level sum reduction (variant 3),CUDA C++
144,__syncthreads,Write a kernel that uses shared memory and __syncthreads to compute a block-level sum reduction (variant 4),CUDA C++
145,__syncthreads,Write a kernel that uses __syncthreads to synchronize before and after writing to shared memory,CUDA C++
146,__syncthreads,Write a kernel that uses __syncthreads to perform a block-level prefix sum,CUDA C++
147,__syncthreads,Write a kernel that uses __syncthreads to find the maximum value in a block,CUDA C++
148,__syncthreads,Write a kernel that uses __syncthreads to find the minimum value in a block,CUDA C++
149,__syncthreads,Write a kernel that uses __syncthreads to transpose a tile in shared memory,CUDA C++
150,__syncthreads,Write a kernel that uses __syncthreads to perform a block-level matrix multiplication,CUDA C++
151,__syncthreads,Write a kernel that uses __syncthreads to load halo regions for stencil computation,CUDA C++
152,__syncthreads,Write a kernel that demonstrates the danger of __syncthreads inside conditional code,CUDA C++
153,__syncthreads,Write a kernel that uses __syncthreads_count to count threads meeting a condition,CUDA C++
154,__syncthreads,Write a kernel that uses __syncthreads_and to check if all threads meet a condition,CUDA C++
155,__syncthreads,Write a kernel that uses __syncthreads_or to check if any thread meets a condition,CUDA C++
156,cudaDeviceSynchronize,Write a program that launches a kernel and uses cudaDeviceSynchronize to wait for completion (variant 1),CUDA C++
157,cudaDeviceSynchronize,Write a program that launches a kernel and uses cudaDeviceSynchronize to wait for completion (variant 2),CUDA C++
158,cudaDeviceSynchronize,Write a program that launches a kernel and uses cudaDeviceSynchronize to wait for completion (variant 3),CUDA C++
159,cudaDeviceSynchronize,Write a program that launches a kernel and uses cudaDeviceSynchronize to wait for completion (variant 4),CUDA C++
160,cudaDeviceSynchronize,Launch multiple kernels sequentially with cudaDeviceSynchronize between each,CUDA C++
161,cudaDeviceSynchronize,"Launch a kernel, do host work, then cudaDeviceSynchronize before reading results",CUDA C++
162,cudaDeviceSynchronize,Use cudaDeviceSynchronize to ensure all streams complete before exiting,CUDA C++
163,cudaDeviceSynchronize,Compare cudaDeviceSynchronize vs stream synchronize for a single stream,CUDA C++
164,Global memory,Write a kernel that reads from global memory with coalesced access patterns (variant 1),CUDA C++
165,Global memory,Write a kernel that reads from global memory with coalesced access patterns (variant 2),CUDA C++
166,Global memory,Write a kernel that reads from global memory with coalesced access patterns (variant 3),CUDA C++
167,Global memory,Write a kernel that reads from global memory with coalesced access patterns (variant 4),CUDA C++
168,Global memory,Write a kernel that reads from global memory with strided (non-coalesced) access patterns,CUDA C++
169,Global memory,Write a kernel that writes to global memory with coalesced patterns,CUDA C++
170,Global memory,Write a kernel that uses pointer chasing through global memory and measure latency,CUDA C++
171,Global memory,Write a kernel that accesses global memory with a large offset and observe performance,CUDA C++
172,Global memory,"Write a kernel that loads from global memory into registers, then processes and stores back",CUDA C++
173,Global memory,Write a kernel that demonstrates the effect of misaligned global memory access,CUDA C++
174,Global memory,Write a kernel that uses float4 to load 4 floats at once from global memory,CUDA C++
175,Global memory,Write a kernel that uses int4 to load 4 integers at once from global memory,CUDA C++
176,Global memory,Write a kernel that compares bandwidth of read-only vs read-write global memory access,CUDA C++
177,Global memory,Write a kernel that uses __ldg intrinsic for read-only global memory loads,CUDA C++
178,Shared memory,Write a kernel that uses __shared__ to cache tile data for faster computation (variant 1),CUDA C++
179,Shared memory,Write a kernel that uses __shared__ to cache tile data for faster computation (variant 2),CUDA C++
180,Shared memory,Write a kernel that uses __shared__ to cache tile data for faster computation (variant 3),CUDA C++
181,Shared memory,Write a kernel that uses __shared__ to cache tile data for faster computation (variant 4),CUDA C++
182,Shared memory,Write a kernel that uses __shared__ to cache tile data for faster computation (variant 5),CUDA C++
183,Shared memory,Write a kernel that uses shared memory to perform a 1D stencil operation,CUDA C++
184,Shared memory,Write a kernel that uses shared memory to perform a 2D stencil operation,CUDA C++
185,Shared memory,Write a kernel that uses shared memory to compute a block-level matrix-vector product,CUDA C++
186,Shared memory,Write a kernel that dynamically allocates shared memory at launch time,CUDA C++
187,Shared memory,Write a kernel that uses shared memory to perform a block-level sort,CUDA C++
188,Shared memory,Write a kernel that uses shared memory to cache lookup table data,CUDA C++
189,Shared memory,Write a kernel that uses shared memory for double buffering (two buffers),CUDA C++
190,Shared memory,Write a kernel that uses shared memory to compute a convolution filter,CUDA C++
191,Shared memory,Write a kernel that bank-conflict-free accesses shared memory,CUDA C++
192,Shared memory,Write a kernel that intentionally causes shared memory bank conflicts and measures impact,CUDA C++
193,Shared memory,Write a kernel that uses shared memory for inter-thread communication within a block,CUDA C++
194,Constant memory,Write a kernel that uses __constant__ memory to store a filter kernel for convolution (variant 1),CUDA C++
195,Constant memory,Write a kernel that uses __constant__ memory to store a filter kernel for convolution (variant 2),CUDA C++
196,Constant memory,Write a kernel that uses __constant__ memory to store a filter kernel for convolution (variant 3),CUDA C++
197,Constant memory,Write a kernel that uses __constant__ memory to store a lookup table of 256 values,CUDA C++
198,Constant memory,Write a kernel that uses __constant__ memory to store a small matrix (4x4),CUDA C++
199,Constant memory,Write a kernel that uses __constant__ memory for coefficients of a polynomial,CUDA C++
200,Constant memory,Compare performance of constant memory vs global memory for read-only small data,CUDA C++
201,Constant memory,Write a kernel that updates constant memory between kernel launches,CUDA C++
202,Registers,Write a kernel that efficiently uses registers to compute Fibonacci numbers for each thread (variant 1),CUDA C++
203,Registers,Write a kernel that efficiently uses registers to compute Fibonacci numbers for each thread (variant 2),CUDA C++
204,Registers,Write a kernel that efficiently uses registers to compute Fibonacci numbers for each thread (variant 3),CUDA C++
205,Registers,Write a kernel that uses many registers and observe register spilling to local memory,CUDA C++
206,Registers,Write a kernel that minimizes register usage and compare occupancy,CUDA C++
207,Registers,Write a kernel that stores temporary variables in registers for a dot product,CUDA C++
208,Registers,Write a kernel that uses register arrays (if supported) for small fixed-size storage,CUDA C++
209,atomicAdd,Write a kernel where multiple threads atomically add to a shared counter and verify the final result (variant 1),CUDA C++
210,atomicAdd,Write a kernel where multiple threads atomically add to a shared counter and verify the final result (variant 2),CUDA C++
211,atomicAdd,Write a kernel where multiple threads atomically add to a shared counter and verify the final result (variant 3),CUDA C++
212,atomicAdd,Write a kernel where multiple threads atomically add to a shared counter and verify the final result (variant 4),CUDA C++
213,atomicAdd,Use atomicAdd to compute the sum of an array using multiple blocks,CUDA C++
214,atomicAdd,Use atomicAdd with floats to accumulate a sum and compare precision vs integer,CUDA C++
215,atomicAdd,Use atomicAdd to compute a histogram with 10 bins,CUDA C++
216,atomicAdd,Use atomicAdd to implement a work queue where threads claim tasks,CUDA C++
217,atomicAdd,Compare atomicAdd performance vs reduction for summing large arrays,CUDA C++
218,atomicAdd,Use atomicAdd to count the number of elements satisfying a condition,CUDA C++
219,atomicAdd,Use atomicAdd to accumulate partial sums from each warp,CUDA C++
220,atomicCAS,Use atomicCAS to implement a spin-lock that protects access to a shared variable (variant 1),CUDA C++
221,atomicCAS,Use atomicCAS to implement a spin-lock that protects access to a shared variable (variant 2),CUDA C++
222,atomicCAS,Use atomicCAS to implement a spin-lock that protects access to a shared variable (variant 3),CUDA C++
223,atomicCAS,Use atomicCAS to implement a compare-and-swap loop for updating a maximum value,CUDA C++
224,atomicCAS,Use atomicCAS to implement a lock-free stack push operation,CUDA C++
225,atomicCAS,Use atomicCAS to implement a lock-free queue enqueue operation,CUDA C++
226,atomicCAS,Use atomicCAS to implement an atomic minimum operation,CUDA C++
227,atomicCAS,Use atomicCAS to implement an atomic maximum operation,CUDA C++
228,atomicCAS,Use atomicCAS to implement a ticket lock for fair synchronization,CUDA C++
229,atomicCAS,Compare performance of atomicCAS spin-lock vs atomicAdd for simple counters,CUDA C++
230,atomicMin,Use atomicMin to find the global minimum value across all threads,CUDA C++
231,atomicMax,Use atomicMax to find the global maximum value across all threads,CUDA C++
232,atomicExch,Use atomicExch to implement a simple semaphore,CUDA C++
233,atomicInc,Use atomicInc to cycle through a circular buffer index,CUDA C++
234,atomicDec,Use atomicDec to decrement a counter with wraparound,CUDA C++
235,cudaStream_t,Create two streams and launch kernels on both streams to overlap computation (variant 1),CUDA C++
236,cudaStream_t,Create two streams and launch kernels on both streams to overlap computation (variant 2),CUDA C++
237,cudaStream_t,Create two streams and launch kernels on both streams to overlap computation (variant 3),CUDA C++
238,cudaStream_t,Create two streams and overlap kernel execution with data transfer in each,CUDA C++
239,cudaStream_t,"Create a stream, launch a kernel, then query its completion status",CUDA C++
240,cudaStream_t,"Create a stream, launch a kernel, then synchronize only that stream",CUDA C++
241,cudaStream_t,Create a priority stream and compare execution order with normal streams,CUDA C++
242,cudaStream_t,Create a stream with CUDA stream capture for graph execution,CUDA C++
243,cudaStream_t,Use cudaStreamWaitEvent to synchronize two streams at a specific point,CUDA C++
244,cudaStream_t,Use cudaStreamAttachMemAsync for unified memory on multi-GPU systems,CUDA C++
245,cudaStream_t,Destroy a stream and verify all operations completed before destruction,CUDA C++
246,cudaStream_t,Create a stream per GPU and launch kernels on each GPU concurrently,CUDA C++
247,cudaStreamCreate,Create 4 CUDA streams and demonstrate async operations on each stream (variant 1),CUDA C++
248,cudaStreamCreate,Create 4 CUDA streams and demonstrate async operations on each stream (variant 2),CUDA C++
249,cudaStreamCreate,Create 4 CUDA streams and demonstrate async operations on each stream (variant 3),CUDA C++
250,cudaStreamCreate,Create 8 streams and distribute array processing across all streams,CUDA C++
251,cudaStreamCreate,Create streams with different priorities using cudaStreamCreateWithPriority,CUDA C++
252,cudaStreamCreate,"Create a stream, use it for async memcpy, then reuse it for a kernel launch",CUDA C++
253,cudaStreamCreate,Create streams in a loop and store them in an array for later use,CUDA C++
254,cudaStreamCreate,Create a stream with non-blocking sync behavior,CUDA C++
255,cudaStreamCreate,Create and destroy streams in a loop to test resource management,CUDA C++
256,cudaEvent_t,Use CUDA events to measure the execution time of a kernel (variant 1),CUDA C++
257,cudaEvent_t,Use CUDA events to measure the execution time of a kernel (variant 2),CUDA C++
258,cudaEvent_t,Use CUDA events to measure the execution time of a kernel (variant 3),CUDA C++
259,cudaEvent_t,Use CUDA events to measure host-to-device transfer time,CUDA C++
260,cudaEvent_t,Use CUDA events to measure device-to-host transfer time,CUDA C++
261,cudaEvent_t,Use CUDA events to measure total time of a sequence of kernel launches,CUDA C++
262,cudaEvent_t,Use CUDA events with inter-stream timing to measure concurrent execution,CUDA C++
263,cudaEvent_t,Use CUDA events to synchronize streams at specific points,CUDA C++
264,cudaEvent_t,Use CUDA events to record timestamps and calculate intervals between operations,CUDA C++
265,cudaEvent_t,Use CUDA events with cudaEventBlockingSync to avoid CPU spin-waiting,CUDA C++
266,cudaEventElapsedTime,Measure and compare the execution time of two different matrix multiplication kernels (variant 1),CUDA C++
267,cudaEventElapsedTime,Measure and compare the execution time of two different matrix multiplication kernels (variant 2),CUDA C++
268,cudaEventElapsedTime,Measure and compare the execution time of two different matrix multiplication kernels (variant 3),CUDA C++
269,cudaEventElapsedTime,Measure elapsed time for a reduction kernel vs CPU reduction,CUDA C++
270,cudaEventElapsedTime,Measure elapsed time for shared memory vs global memory kernel,CUDA C++
271,cudaEventElapsedTime,Measure elapsed time for different block sizes of the same kernel,CUDA C++
272,Memory coalescing,Write two kernels that access memory in coalesced and non-coalesced patterns and compare performance (variant 1),CUDA C++
273,Memory coalescing,Write two kernels that access memory in coalesced and non-coalesced patterns and compare performance (variant 2),CUDA C++
274,Memory coalescing,Write two kernels that access memory in coalesced and non-coalesced patterns and compare performance (variant 3),CUDA C++
275,Memory coalescing,Write a kernel with coalesced float4 loads and compare with float loads,CUDA C++
276,Memory coalescing,Write a kernel that transposes data to achieve coalesced access in a second kernel,CUDA C++
277,Memory coalescing,Write a kernel that uses shared memory to convert non-coalesced to coalesced access,CUDA C++
278,Memory coalescing,Write a kernel that demonstrates the impact of misalignment on coalescing,CUDA C++
279,Memory coalescing,Write a kernel that uses AoS vs SoA layout and compare coalescing efficiency,CUDA C++
280,Memory coalescing,Write a kernel with bank-conflict-free shared memory access patterns,CUDA C++
281,Memory coalescing,Write a kernel that pads shared memory arrays to avoid bank conflicts,CUDA C++
282,__shfl_sync,Use warp shuffle operations to compute the sum of all values in a warp (variant 1),CUDA C++
283,__shfl_sync,Use warp shuffle operations to compute the sum of all values in a warp (variant 2),CUDA C++
284,__shfl_sync,Use warp shuffle operations to compute the sum of all values in a warp (variant 3),CUDA C++
285,__shfl_sync,Use warp shuffle to perform a warp-level broadcast of a single value,CUDA C++
286,__shfl_sync,Use warp shuffle to perform a warp-level reduction for maximum value,CUDA C++
287,__shfl_sync,Use warp shuffle to perform a warp-level reduction for minimum value,CUDA C++
288,__shfl_sync,Use warp shuffle to exchange data between adjacent threads in a warp,CUDA C++
289,__shfl_sync,Use warp shuffle to perform a warp-level prefix sum,CUDA C++
290,__shfl_sync,Compare warp shuffle reduction vs shared memory reduction performance,CUDA C++
291,__warpSize,Write a kernel that uses __warpSize to optimize warp-level reductions (variant 1),CUDA C++
292,__warpSize,Write a kernel that uses __warpSize to optimize warp-level reductions (variant 2),CUDA C++
293,__warpSize,Write a kernel that uses __warpSize to handle arbitrary warp sizes portably,CUDA C++
294,__warpSize,Write a kernel that uses __warpSize to determine loop bounds in warp reductions,CUDA C++
295,__warpSize,Write a kernel that handles the case when array size is not a multiple of warpSize,CUDA C++
296,cudaGetLastError,Write a program that intentionally causes an error and uses cudaGetLastError to detect and report it (variant 1),CUDA C++
297,cudaGetLastError,Write a program that intentionally causes an error and uses cudaGetLastError to detect and report it (variant 2),CUDA C++
298,cudaGetLastError,Write a program that intentionally causes an error and uses cudaGetLastError to detect and report it (variant 3),CUDA C++
299,cudaGetLastError,Write a program that checks cudaGetLastError after every CUDA API call,CUDA C++
300,cudaGetLastError,Write a macro that wraps CUDA calls and automatically checks cudaGetLastError,CUDA C++
301,cudaGetLastError,Write a function that converts cudaError_t to a human-readable string,CUDA C++
302,cudaGetLastError,Write a program that clears a sticky error with cudaGetLastError and continues execution,CUDA C++
303,cudaGetLastError,Write a program that uses cudaPeekAtLastError vs cudaGetLastError and shows the difference,CUDA C++
304,cudaGetLastError,Write a program that handles out-of-memory errors gracefully,CUDA C++
305,cudaGetLastError,Write a program that handles invalid device pointer errors,CUDA C++
306,cudaGetLastError,Write a program that handles kernel launch failure due to too many resources,CUDA C++
307,printf in kernels,Write a kernel that uses printf to debug thread indices and values (variant 1),CUDA C++
308,printf in kernels,Write a kernel that uses printf to debug thread indices and values (variant 2),CUDA C++
309,printf in kernels,Write a kernel that uses printf to debug thread indices and values (variant 3),CUDA C++
310,printf in kernels,Write a kernel that uses printf to print the values of shared memory before and after sync,CUDA C++
311,printf in kernels,Write a kernel that uses printf conditionally (only for thread 0) to reduce output,CUDA C++
312,printf in kernels,Write a kernel that uses printf to trace execution flow through a reduction,CUDA C++
313,printf in kernels,Write a kernel that uses printf to debug a bank conflict in shared memory,CUDA C++
314,printf in kernels,Write a kernel that uses printf to verify coalesced memory access patterns,CUDA C++
315,Nsight Compute,Profile a simple matrix multiplication kernel using NVIDIA Nsight Compute (variant 1),CUDA C++
316,Nsight Compute,Profile a simple matrix multiplication kernel using NVIDIA Nsight Compute (variant 2),CUDA C++
317,Nsight Compute,Profile a reduction kernel with Nsight Compute and analyze memory throughput,CUDA C++
318,Nsight Compute,Profile a kernel with Nsight Compute and identify register pressure issues,CUDA C++
319,Nsight Compute,Profile a kernel with Nsight Compute and analyze occupancy metrics,CUDA C++
320,Nsight Compute,Profile a kernel with Nsight Compute and check for memory coalescing efficiency,CUDA C++
321,Nsight Systems,Trace a multi-kernel CUDA program using NVIDIA Nsight Systems to visualize timeline (variant 1),CUDA C++
322,Nsight Systems,Trace a multi-kernel CUDA program using NVIDIA Nsight Systems to visualize timeline (variant 2),CUDA C++
323,Nsight Systems,Use Nsight Systems to visualize concurrent kernel execution across multiple streams,CUDA C++
324,Nsight Systems,Use Nsight Systems to identify gaps between kernel launches and optimize,CUDA C++
325,Nsight Systems,Use Nsight Systems to trace CPU-GPU synchronization overhead,CUDA C++
326,cuBLAS gemm,Use cuBLAS to multiply two 1024x1024 matrices and compare with CPU implementation (variant 1),CUDA C++
327,cuBLAS gemm,Use cuBLAS to multiply two 1024x1024 matrices and compare with CPU implementation (variant 2),CUDA C++
328,cuBLAS gemm,Use cuBLAS to multiply two 1024x1024 matrices and compare with CPU implementation (variant 3),CUDA C++
329,cuBLAS gemm,Use cuBLAS to multiply two non-square matrices (1024x512 and 512x2048),CUDA C++
330,cuBLAS gemm,Use cuBLAS with column-major layout and handle row-major input conversion,CUDA C++
331,cuBLAS gemm,Use cuBLAS with batched matrix multiplication for multiple small matrices,CUDA C++
332,cuBLAS gemm,Use cuBLAS with strided batched matrix multiplication,CUDA C++
333,cuBLAS gemm,"Use cuBLAS with mixed precision (FP16 input, FP32 accumulation)",CUDA C++
334,cuBLAS gemm,Use cuBLAS with Tensor Core acceleration by setting math mode,CUDA C++
335,cuBLAS gemv,Use cuBLAS to multiply a 1000x1000 matrix by a 1000-element vector (variant 1),CUDA C++
336,cuBLAS gemv,Use cuBLAS to multiply a 1000x1000 matrix by a 1000-element vector (variant 2),CUDA C++
337,cuBLAS gemv,Use cuBLAS to multiply a 1000x1000 matrix by a 1000-element vector (variant 3),CUDA C++
338,cuBLAS gemv,Use cuBLAS to multiply a non-square matrix by a vector,CUDA C++
339,cuBLAS gemv,Use cuBLAS for a batched matrix-vector product,CUDA C++
340,cuBLAS gemv,Compare cuBLAS gemv performance with a custom CUDA kernel,CUDA C++
341,cuRAND,Generate 1 million random floats on the GPU using cuRAND and compute their mean (variant 1),CUDA C++
342,cuRAND,Generate 1 million random floats on the GPU using cuRAND and compute their mean (variant 2),CUDA C++
343,cuRAND,Generate random integers on the GPU using cuRAND and compute their histogram,CUDA C++
344,cuRAND,Generate normally distributed random numbers using cuRAND,CUDA C++
345,cuRAND,Generate random numbers with cuRAND and use them in a Monte Carlo simulation,CUDA C++
346,cuRAND,Set up cuRAND with a specific seed and verify reproducibility,CUDA C++
347,cuRAND,Use cuRAND with multiple generators for different streams,CUDA C++
348,Tensor Cores,Write a kernel using wmma API to perform matrix multiplication leveraging Tensor Cores (variant 1),CUDA C++
349,Tensor Cores,Write a kernel using wmma API to perform matrix multiplication leveraging Tensor Cores (variant 2),CUDA C++
350,Tensor Cores,Write a kernel using wmma API to perform matrix multiplication leveraging Tensor Cores (variant 3),CUDA C++
351,Tensor Cores,Use wmma API with FP16 inputs and FP32 accumulation,CUDA C++
352,Tensor Cores,Use wmma API with INT8 inputs and INT32 accumulation,CUDA C++
353,Tensor Cores,Use wmma API to perform a batched matrix multiplication,CUDA C++
354,Tensor Cores,Compare performance of wmma Tensor Cores vs standard CUDA for small matrices,CUDA C++
355,Tensor Cores,Compare performance of wmma Tensor Cores vs standard CUDA for large matrices,CUDA C++
356,Tensor Cores,Use wmma with fragment loading and storing to global memory,CUDA C++
357,Occupancy,Analyze a kernel and calculate theoretical occupancy given its register and shared memory usage (variant 1),CUDA C++
358,Occupancy,Analyze a kernel and calculate theoretical occupancy given its register and shared memory usage (variant 2),CUDA C++
359,Occupancy,Analyze a kernel and calculate theoretical occupancy given its register and shared memory usage (variant 3),CUDA C++
360,Occupancy,Use cudaOccupancyMaxPotentialBlockSize to find optimal block size for a kernel,CUDA C++
361,Occupancy,Use cudaOccupancyMaxActiveBlocksPerMultiprocessor to determine active blocks,CUDA C++
362,Occupancy,Vary shared memory usage and observe the impact on occupancy and performance,CUDA C++
363,Occupancy,Vary register usage and observe the impact on occupancy and performance,CUDA C++
364,Occupancy,Launch a kernel with limited occupancy and compare with a high-occupancy version,CUDA C++
365,Occupancy,Use launch bounds to limit registers and increase occupancy,CUDA C++
366,Roofline model,Measure memory bandwidth and compute throughput of your GPU using a simple kernel (variant 1),CUDA C++
367,Roofline model,Measure memory bandwidth and compute throughput of your GPU using a simple kernel (variant 2),CUDA C++
368,Roofline model,Plot a roofline model for your GPU using measured bandwidth and FLOPS,CUDA C++
369,Roofline model,Determine if a matrix multiplication kernel is compute-bound or memory-bound,CUDA C++
370,Roofline model,Determine if a reduction kernel is compute-bound or memory-bound,CUDA C++
371,Roofline model,Optimize a memory-bound kernel to move closer to the compute roofline,CUDA C++
372,Peer-to-peer,Copy data directly between GPU 0 and GPU 1 using peer-to-peer transfers (variant 1),CUDA C++
373,Peer-to-peer,Copy data directly between GPU 0 and GPU 1 using peer-to-peer transfers (variant 2),CUDA C++
374,Peer-to-peer,Copy data directly between GPU 0 and GPU 1 using peer-to-peer transfers (variant 3),CUDA C++
375,Peer-to-peer,Enable peer-to-peer access between two GPUs and check if it is supported,CUDA C++
376,Peer-to-peer,Copy data between GPUs using peer-to-peer vs host staging and compare bandwidth,CUDA C++
377,Peer-to-peer,Use peer-to-peer access to read data from another GPU's memory in a kernel,CUDA C++
378,Peer-to-peer,Disable peer-to-peer access after use and verify cleanup,CUDA C++
379,Peer-to-peer,Use peer-to-peer with NVLink vs PCIe and compare bandwidth if available,CUDA C++
380,Peer-to-peer,Use peer-to-peer for direct GPU-GPU communication in a multi-GPU reduction,CUDA C++
381,Multi-GPU reduction,Write a program that reduces data across two GPUs and aggregates the result on the host (variant 1),CUDA C++
382,Multi-GPU reduction,Write a program that reduces data across two GPUs and aggregates the result on the host (variant 2),CUDA C++
383,Multi-GPU reduction,Write a program that reduces data across two GPUs and aggregates the result on the host (variant 3),CUDA C++
384,Multi-GPU reduction,Write a program that reduces data across four GPUs using peer-to-peer aggregation,CUDA C++
385,Multi-GPU reduction,Write a program that splits an array across two GPUs and sums partial results on host,CUDA C++
386,Multi-GPU reduction,Write a program that performs all-reduce across multiple GPUs using tree-based reduction,CUDA C++
387,Multi-GPU reduction,Write a program that overlaps computation on multiple GPUs with CPU work,CUDA C++
388,Multi-GPU reduction,Write a program that uses OpenMP threads to control different GPUs,CUDA C++
389,Multi-GPU reduction,Write a program that uses MPI ranks to control different GPUs in a cluster,CUDA C++
390,Multi-GPU reduction,Write a program that handles different GPU architectures in a multi-GPU setup,CUDA C++
391,Dynamic parallelism,Write a nested kernel where a parent kernel launches child kernels dynamically (variant 1),CUDA C++
392,Dynamic parallelism,Write a nested kernel where a parent kernel launches child kernels dynamically (variant 2),CUDA C++
393,Dynamic parallelism,Write a nested kernel where a parent kernel launches child kernels dynamically (variant 3),CUDA C++
394,Dynamic parallelism,Use dynamic parallelism to recursively process a tree structure on the GPU,CUDA C++
395,Dynamic parallelism,Use dynamic parallelism to implement a adaptive grid refinement,CUDA C++
396,Dynamic parallelism,Use dynamic parallelism to launch different kernels based on runtime data,CUDA C++
397,Dynamic parallelism,Compare performance of dynamic parallelism vs flat grid launch for the same problem,CUDA C++
398,Dynamic parallelism,Use dynamic parallelism with synchronization between parent and child kernels,CUDA C++
399,Dynamic parallelism,Use dynamic parallelism to implement a GPU-side quicksort,CUDA C++
400,Cooperative groups,Use cooperative_groups API to perform a grid-level reduction (variant 1),CUDA C++
401,Cooperative groups,Use cooperative_groups API to perform a grid-level reduction (variant 2),CUDA C++
402,Cooperative groups,Use cooperative_groups API to perform a grid-level reduction (variant 3),CUDA C++
403,Cooperative groups,Use cooperative_groups to perform a warp-level reduction without shared memory,CUDA C++
404,Cooperative groups,Use cooperative_groups to perform a block-level reduction with sync,CUDA C++
405,Cooperative groups,Use cooperative_groups to partition threads into tiles for matrix operations,CUDA C++
406,Cooperative groups,Use cooperative_groups to implement a grid-level prefix sum,CUDA C++
407,Cooperative groups,Use cooperative_groups to synchronize across the entire grid,CUDA C++
408,Cooperative groups,Use cooperative_groups to implement a multi-grid reduction across multiple kernels,CUDA C++
409,Cooperative groups,Compare cooperative_groups reduction vs traditional shared memory reduction,CUDA C++
410,Unified Memory,Use cudaMallocManaged to allocate unified memory and access it from both host and device (variant 1),CUDA C++
411,Unified Memory,Use cudaMallocManaged to allocate unified memory and access it from both host and device (variant 2),CUDA C++
412,Unified Memory,Use cudaMallocManaged to allocate unified memory and access it from both host and device (variant 3),CUDA C++
413,Unified Memory,Use cudaMallocManaged to allocate unified memory and access it from both host and device (variant 4),CUDA C++
414,Unified Memory,Use cudaMallocManaged for a struct with nested pointers and handle deep copy,CUDA C++
415,Unified Memory,Use cudaMallocManaged for a 2D array and access it with 2D indexing,CUDA C++
416,Unified Memory,Use cudaMallocManaged and over-allocate to trigger eviction and observe performance,CUDA C++
417,Unified Memory,Use cudaMallocManaged on a multi-GPU system and observe automatic migration,CUDA C++
418,Unified Memory,Use cudaMallocManaged with advice hints (cudaMemAdvise) for read-mostly data,CUDA C++
419,Unified Memory,Use cudaMallocManaged with cudaMemAdviseSetPreferredLocation for specific GPU,CUDA C++
420,Unified Memory,Use cudaMallocManaged with cudaMemAdviseSetAccessedBy for performance hints,CUDA C++
421,Unified Memory,Compare performance of unified memory vs explicit cudaMemcpy for repeated access,CUDA C++
422,Unified Memory,Use cudaMallocManaged to implement a linked list accessible from GPU,CUDA C++
423,Unified Memory,Profile unified memory page faults and migrations using Nsight Systems,CUDA C++
424,Memory prefetch,Use cudaMemPrefetchAsync to prefetch managed memory to a GPU before kernel execution (variant 1),CUDA C++
425,Memory prefetch,Use cudaMemPrefetchAsync to prefetch managed memory to a GPU before kernel execution (variant 2),CUDA C++
426,Memory prefetch,Use cudaMemPrefetchAsync to prefetch managed memory to a GPU before kernel execution (variant 3),CUDA C++
427,Memory prefetch,Use cudaMemPrefetchAsync to prefetch data to multiple GPUs in a multi-GPU setup,CUDA C++
428,Memory prefetch,Use cudaMemPrefetchAsync to prefetch data back to host after kernel execution,CUDA C++
429,Memory prefetch,Compare performance with and without prefetching for a memory-intensive kernel,CUDA C++
430,Memory prefetch,Use cudaMemPrefetchAsync with streams to overlap prefetching and computation,CUDA C++
431,Memory prefetch,Use cudaMemPrefetchAsync with advice hints for optimal prefetching,CUDA C++
432,Memory prefetch,Measure page fault overhead with and without prefetching using Nsight Systems,CUDA C++
433,2D kernel launch,Write a kernel for 2D convolution using 2D block and grid organization (variant 1),CUDA C++
434,2D kernel launch,Write a kernel for 2D convolution using 2D block and grid organization (variant 2),CUDA C++
435,2D kernel launch,Write a kernel for 2D convolution using 2D block and grid organization (variant 3),CUDA C++
436,2D kernel launch,Write a kernel for 2D image blur using 2D blocks and shared memory tiling,CUDA C++
437,2D kernel launch,Write a kernel for 2D image sharpening using 2D convolution,CUDA C++
438,2D kernel launch,Write a kernel for 2D matrix transpose using 2D thread blocks,CUDA C++
439,2D kernel launch,Write a kernel for 2D Sobel edge detection using 2D blocks,CUDA C++
440,2D kernel launch,Write a kernel for 2D separable convolution using two 1D passes,CUDA C++
441,2D kernel launch,Write a kernel for 3D volume rendering using 3D blocks,CUDA C++
442,2D kernel launch,Write a kernel for 2D Jacobi iteration using 2D blocks and halo regions,CUDA C++
443,Reduce operation,Implement a reduction kernel that computes the sum of all elements in an array (variant 1),CUDA C++
444,Reduce operation,Implement a reduction kernel that computes the sum of all elements in an array (variant 2),CUDA C++
445,Reduce operation,Implement a reduction kernel that computes the sum of all elements in an array (variant 3),CUDA C++
446,Reduce operation,Implement a reduction kernel that computes the sum of all elements in an array (variant 4),CUDA C++
447,Reduce operation,Implement a reduction kernel that computes the sum of all elements in an array (variant 5),CUDA C++
448,Reduce operation,Implement a reduction kernel that computes the maximum of all elements,CUDA C++
449,Reduce operation,Implement a reduction kernel that computes the minimum of all elements,CUDA C++
450,Reduce operation,Implement a reduction kernel that computes the sum of squares,CUDA C++
451,Reduce operation,Implement a reduction kernel that computes the dot product of two arrays,CUDA C++
452,Reduce operation,Implement a multi-block reduction with atomicAdd to combine partial results,CUDA C++
453,Reduce operation,Implement a warp-shuffle reduction without shared memory,CUDA C++
454,Reduce operation,Implement a recursive reduction that halves the array each iteration,CUDA C++
455,Reduce operation,Implement a reduction using cooperative_groups for grid-level sync,CUDA C++
456,Reduce operation,Implement a reduction using CUB library for comparison,CUDA C++
457,Reduce operation,Implement a segmented reduction for multiple independent arrays,CUDA C++
458,Reduce operation,Implement a reduction with loop unrolling for the last warp,CUDA C++
459,Reduce operation,Implement a reduction with template unrolling for different block sizes,CUDA C++
460,Reduce operation,Implement a reduction that handles non-power-of-2 array sizes,CUDA C++
461,Reduce operation,Implement a reduction using shared memory with bank-conflict-free access,CUDA C++
462,Prefix sum,Implement an inclusive scan/prefix sum kernel for an array of integers (variant 1),CUDA C++
463,Prefix sum,Implement an inclusive scan/prefix sum kernel for an array of integers (variant 2),CUDA C++
464,Prefix sum,Implement an inclusive scan/prefix sum kernel for an array of integers (variant 3),CUDA C++
465,Prefix sum,Implement an inclusive scan/prefix sum kernel for an array of integers (variant 4),CUDA C++
466,Prefix sum,Implement an exclusive scan/prefix sum kernel for an array of integers,CUDA C++
467,Prefix sum,Implement a prefix sum using the Blelloch scan algorithm,CUDA C++
468,Prefix sum,Implement a prefix sum using shared memory for intra-block scan,CUDA C++
469,Prefix sum,Implement a multi-block prefix sum with propagation of block sums,CUDA C++
470,Prefix sum,Implement a prefix sum using warp-shuffle operations,CUDA C++
471,Prefix sum,Implement a prefix sum using CUB library for comparison,CUDA C++
472,Prefix sum,Implement a prefix sum for non-power-of-2 array sizes,CUDA C++
473,Prefix sum,Implement a segmented scan for multiple independent segments,CUDA C++
474,Prefix sum,Implement a prefix sum using double buffering to avoid in-place issues,CUDA C++
475,Prefix sum,Implement a prefix sum with 2D data using row-major and column-major scans,CUDA C++
476,Matrix transpose,Write a kernel that efficiently transposes a 2048x2048 matrix using shared memory (variant 1),CUDA C++
477,Matrix transpose,Write a kernel that efficiently transposes a 2048x2048 matrix using shared memory (variant 2),CUDA C++
478,Matrix transpose,Write a kernel that efficiently transposes a 2048x2048 matrix using shared memory (variant 3),CUDA C++
479,Matrix transpose,Write a kernel that efficiently transposes a 2048x2048 matrix using shared memory (variant 4),CUDA C++
480,Matrix transpose,Write a naive matrix transpose and compare with optimized shared memory version,CUDA C++
481,Matrix transpose,Write a matrix transpose that handles non-square matrices,CUDA C++
482,Matrix transpose,Write a matrix transpose using shared memory with padding to avoid bank conflicts,CUDA C++
483,Matrix transpose,Write a matrix transpose using shared memory without padding and measure bank conflicts,CUDA C++
484,Matrix transpose,Write a matrix transpose for a large matrix (8192x8192) using tiling,CUDA C++
485,Matrix transpose,Write a matrix transpose using float4 vector loads for higher bandwidth,CUDA C++
486,Matrix transpose,Write a matrix transpose in-place for a square matrix,CUDA C++
487,Matrix transpose,Write a matrix transpose using cuBLAS for comparison,CUDA C++
488,Matrix transpose,Write a matrix transpose with diagonal tiling for cache optimization,CUDA C++
489,Matrix transpose,Write a matrix transpose for a batch of small matrices,CUDA C++
490,Matrix multiplication,Implement matrix multiplication with optimizations like tiling and loop unrolling (variant 1),CUDA C++
491,Matrix multiplication,Implement matrix multiplication with optimizations like tiling and loop unrolling (variant 2),CUDA C++
492,Matrix multiplication,Implement matrix multiplication with optimizations like tiling and loop unrolling (variant 3),CUDA C++
493,Matrix multiplication,Implement matrix multiplication with optimizations like tiling and loop unrolling (variant 4),CUDA C++
494,Matrix multiplication,Implement a naive matrix multiplication and compare with optimized version,CUDA C++
495,Matrix multiplication,Implement matrix multiplication using shared memory tiling,CUDA C++
496,Matrix multiplication,Implement matrix multiplication using shared memory with double buffering,CUDA C++
497,Matrix multiplication,Implement matrix multiplication using register tiling for sub-blocks,CUDA C++
498,Matrix multiplication,Implement matrix multiplication using warp-level tiling with warp shuffle,CUDA C++
499,Matrix multiplication,Implement matrix multiplication using Tensor Cores with wmma API,CUDA C++
500,Matrix multiplication,Implement matrix multiplication using cuBLAS for comparison,CUDA C++
501,Matrix multiplication,Implement strassen matrix multiplication for large matrices,CUDA C++
502,Matrix multiplication,Implement matrix multiplication for non-square matrices with dynamic block sizing,CUDA C++
503,Matrix multiplication,Implement batched matrix multiplication for multiple small matrices,CUDA C++
504,Matrix multiplication,Implement matrix multiplication with mixed precision (FP16/FP32),CUDA C++
505,Histogram,Write a kernel that computes a histogram of values in an array using atomic operations (variant 1),CUDA C++
506,Histogram,Write a kernel that computes a histogram of values in an array using atomic operations (variant 2),CUDA C++
507,Histogram,Write a kernel that computes a histogram of values in an array using atomic operations (variant 3),CUDA C++
508,Histogram,Write a histogram kernel with shared memory privatization for 256 bins,CUDA C++
509,Histogram,Write a histogram kernel with shared memory privatization for 1024 bins,CUDA C++
510,Histogram,Write a histogram kernel that handles large value ranges by binning into sub-ranges,CUDA C++
511,Histogram,Write a histogram kernel for 2D image data (grayscale),CUDA C++
512,Histogram,Write a histogram kernel for RGB image data with 3 separate histograms,CUDA C++
513,Histogram,Write a histogram kernel using CUB library for comparison,CUDA C++
514,Histogram,Write a histogram kernel that uses warp-level aggregation before atomics,CUDA C++
515,Histogram,Write a histogram kernel with per-block histograms merged at the end,CUDA C++
516,Histogram,Write a histogram kernel for floating-point data with custom binning,CUDA C++
517,Double buffering,Implement double buffering in a kernel to overlap computation and data loading (variant 1),CUDA C++
518,Double buffering,Implement double buffering in a kernel to overlap computation and data loading (variant 2),CUDA C++
519,Double buffering,Implement double buffering in a kernel to overlap computation and data loading (variant 3),CUDA C++
520,Double buffering,Implement triple buffering for a stencil computation kernel,CUDA C++
521,Double buffering,Implement double buffering for a matrix multiplication with shared memory,CUDA C++
522,Double buffering,Implement double buffering for a 1D convolution kernel,CUDA C++
523,Double buffering,Implement double buffering for a reduction kernel loading chunks,CUDA C++
524,Double buffering,Compare performance of single buffer vs double buffer vs triple buffer,CUDA C++
525,Double buffering,Implement double buffering using async memcpy and streams,CUDA C++
526,__restrict__ pointers,Rewrite a memory-intensive kernel using __restrict__ pointers and measure performance difference (variant 1),CUDA C++
527,__restrict__ pointers,Rewrite a memory-intensive kernel using __restrict__ pointers and measure performance difference (variant 2),CUDA C++
528,__restrict__ pointers,Rewrite a memory-intensive kernel using __restrict__ pointers and measure performance difference (variant 3),CUDA C++
529,__restrict__ pointers,Use __restrict__ on multiple input pointers to enable better load scheduling,CUDA C++
530,__restrict__ pointers,Compare assembly output with and without __restrict__ to see compiler optimizations,CUDA C++
531,__restrict__ pointers,Use __restrict__ combined with const for read-only data,CUDA C++
532,__restrict__ pointers,Use __restrict__ on a pointer to a pointer for indirect access optimization,CUDA C++
533,__restrict__ pointers,Measure performance impact of __restrict__ on a memory-bound kernel,CUDA C++
534,__restrict__ pointers,Use __restrict__ on output pointers to enable better store coalescing hints,CUDA C++
535,Grid-stride loops,Write a kernel that uses a grid-stride loop to handle arrays larger than grid size (variant 1),CUDA C++
536,Grid-stride loops,Write a kernel that uses a grid-stride loop to handle arrays larger than grid size (variant 2),CUDA C++
537,Grid-stride loops,Write a kernel that uses a grid-stride loop to handle arrays larger than grid size (variant 3),CUDA C++
538,Grid-stride loops,Write a kernel that uses a grid-stride loop to handle arrays larger than grid size (variant 4),CUDA C++
539,Grid-stride loops,Write a kernel that uses a grid-stride loop to handle arrays larger than grid size (variant 5),CUDA C++
540,Grid-stride loops,Write a grid-stride loop kernel that processes multiple elements per thread with coalesced access,CUDA C++
541,Grid-stride loops,Write a grid-stride loop kernel for 2D data processing,CUDA C++
542,Grid-stride loops,Write a grid-stride loop kernel for 3D data processing,CUDA C++
543,Grid-stride loops,Compare performance of grid-stride loops vs one-element-per-thread for large arrays,CUDA C++
544,Boundary checks,Write a kernel with proper boundary checks for arbitrary array sizes (variant 1),CUDA C++
545,Boundary checks,Write a kernel with proper boundary checks for arbitrary array sizes (variant 2),CUDA C++
546,Boundary checks,Write a kernel with proper boundary checks for arbitrary array sizes (variant 3),CUDA C++
547,Boundary checks,Write a kernel with proper boundary checks for arbitrary array sizes (variant 4),CUDA C++
548,Boundary checks,Write a kernel with proper boundary checks for arbitrary array sizes (variant 5),CUDA C++
549,Boundary checks,Write a kernel with boundary checks for 2D array processing,CUDA C++
550,Boundary checks,Write a kernel with boundary checks for 3D array processing,CUDA C++
551,Boundary checks,Write a kernel with boundary checks for shared memory tile loading with halo,CUDA C++
552,Boundary checks,Write a kernel with boundary checks for reduction on non-power-of-2 arrays,CUDA C++
553,Template kernels,Write a templated CUDA kernel that works with multiple data types (variant 1),CUDA C++
554,Template kernels,Write a templated CUDA kernel that works with multiple data types (variant 2),CUDA C++
555,Template kernels,Write a templated CUDA kernel that works with multiple data types (variant 3),CUDA C++
556,Template kernels,Write a templated CUDA kernel that works with multiple data types (variant 4),CUDA C++
557,Template kernels,Write a templated CUDA kernel that works with multiple data types (variant 5),CUDA C++
558,Template kernels,Write a templated kernel for generic reduction with any numeric type,CUDA C++
559,Template kernels,Write a templated kernel for generic matrix multiplication,CUDA C++
560,Template kernels,Write a templated kernel with template parameters for block size,CUDA C++
561,Template kernels,Write a templated kernel with template parameters for unroll factors,CUDA C++
