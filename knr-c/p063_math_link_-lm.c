#include <stdio.h>
#include <math.h>

int main() {
	double y = exp(10);
	printf("x: %f\n", y);
	double x2 = sin(2);
	double x = sin(45 * M_PI / 180);
	printf("x: %f\n", x);

	double z = pow(2, 5);
	printf("z : %f\n", z);

	return 0;
}
