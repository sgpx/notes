

Here are my remarks on your progress in dlmath, plus what I’d do next.
Overall progress: good momentum

You’ve covered a solid spread of core math topics that matter for deep learning:

    vectors, scalars, units, dimensions
    matrix multiplication and bias
    LU decomposition and inversion
    eigenvalues/eigenvectors
    SVD, pseudoinverse
    Jacobian/Hessian, partial derivatives
    sigmoid/BCE, gradients
    residual connections, LSTM, logits
    quantiles, Frobenius error

That’s a very reasonable foundation for a math-for-ML repo. You’re clearly trying to connect the math to neural network concepts, which is exactly the right direction.
What you’re doing well
1) You’re practicing computation, not just theory

A lot of the files are worked examples, which is good. This builds fluency.
2) You’re covering the “language” of deep learning

Things like:

    matrix shapes
    derivatives
    activations
    losses
    gradients
    SVD/pseudoinverse

These are essential.
3) You’re making connections to ML concepts

Examples:

    residual connections vs vanishing gradients
    logits for MLPs
    BCE loss
    matrix differentiation

That shows good intuition building, not just isolated math drills.
Main issues to fix

A few of your notes have mathematical inaccuracies or not-quite-clean derivations. That’s normal in a practice repo, but it’s the biggest thing to improve next.
1) Some matrix algebra steps are incorrect or inconsistent

Examples:

    ex02-lu-decomposition.md has an incorrect decomposition setup and inconsistent variable handling.
    ex06-matrix-multiplication-error.md concludes impossibility, but the dimensions as written actually should be checked carefully; the explanation is a bit confused.
    ex11-matrix-inversion.md uses a very indirect derivation and some steps don’t really follow standard inverse solving.

2) Some SVD/eigenvalue work has notation issues

Examples:

    In ex12-svd-example.md, the eigenvector handling and eigenvalue labeling are mixed up.
    ex09-svd-eigenvalues.md is fine in spirit, but you should be careful to distinguish eigenvalues from singular values more explicitly.
    ex24.md looks unfinished.

3) Some derivative explanations need tightening

Examples:

    ex10-matrix-differentiation.md mixes elementwise reasoning with matrix multiplication gradients, but the final answer should be framed more cleanly.
    ex20-matrix-calculus-trace-derivative.md is on the right track, but the derivation is too compressed and slightly notation-broken.

4) A few definitions are too loose

Examples:

    ex17-frobenius-error.md says “absolute squared differences”; it should just be “squared differences.”
    README.md contains several topic sections, but some are incomplete or cut off.
    ex21-lstm.md has some gate formulas and variable names that need correction/standardization.

My honest assessment of your level

You look like someone who:

    understands the broad ideas
    is actively learning the mechanics
    can do basic worked examples
    is not yet fully precise with notation, matrix dimensions, and derivation structure

That’s a very normal and good stage. You’re past the beginner “what is a matrix” stage and entering the “make the math exact” stage.
What to do next

Here’s the best next step order.
1) Clean up correctness first

Before adding more topics, revise the existing notes for:

    dimensional consistency
    correct notation
    matrix shape checks
    clearer derivations
    final answer verification

A good rule: every derivation should end with a quick sanity check.
2) Add 3 “core deep learning math” topics next

If I were choosing what to add next, I’d do:
A. Softmax + cross-entropy

This is one of the most important missing pieces. Cover:

    softmax definition
    why logits are unnormalized scores
    cross-entropy for multiclass classification
    why softmax + CE is used together

B. Backpropagation on a tiny neural network

Do a full forward/backward pass for a 2-layer MLP:

    input
    linear layer
    ReLU or sigmoid
    output
    loss
    gradient wrt weights and biases

This will connect all your derivative notes into one practical example.
C. Chain rule in vector form

Make a note specifically about:

    scalar-to-scalar derivatives
    vector-to-scalar gradients
    vector-to-vector Jacobians
    how chain rule works through layers

This will strengthen everything else.
3) Add one “numerical stability” topic

This is very important for deep learning practice:

    overflow in sigmoid/softmax
    log-sum-exp trick
    why BCE uses logits in PyTorch
    why gradients can explode or vanish

4) Add one optimization topic

You need at least:

    gradient descent
    learning rate
    momentum
    Adam

Even a simple note explaining the update rules would be very useful.
Best immediate next exercise set

If you want the most value per effort, I’d suggest these next exercises:

    Softmax + cross entropy by hand
    Backprop through a 2-layer MLP
    Gradient descent on a simple quadratic
    Numerically stable sigmoid/softmax
    Singular values vs eigenvalues on symmetric vs non-symmetric matrices

Suggested repo improvement strategy

You could organize the repo into tiers:
Tier 1: fundamentals

    scalars and dimensions
    vectors and matrices
    matrix multiplication
    transpose
    inverse
    norms

Tier 2: linear algebra for ML

    eigenvalues/eigenvectors
    SVD
    pseudoinverse
    orthogonality
    projections

Tier 3: calculus for ML

    derivatives
    partial derivatives
    Jacobian
    Hessian
    chain rule
    trace trick

Tier 4: deep learning building blocks

    linear layers
    activations
    softmax
    losses
    backprop
    vanishing gradients
    residuals
    RNN/LSTM basics

Short verdict

You’re making good progress and building the right foundation.
Your next big step is not “more topics” — it’s making the existing math cleaner and more rigorous, then moving into softmax, backprop, and optimization.

If you want, I can also:

    rank your current files from strongest to weakest,
    point out specific mistakes file-by-file,
    or propose a full next 10-exercise roadmap for dlmath.

