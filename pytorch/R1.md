~~Defines a tiny feed‑forward network (2‑layer linear + ReLU) and trains it for 5 epochs on a 2‑D synthetic dataset.~~
~~Implements a manual linear regression loop (y = wx + b) using autograd, with a simple SGD optimizer and prints loss each epoch.~~
~~Uses torch.nn.functional.one_hot to convert class indices to one‑hot vectors and shows how to feed them into a model.~~

~~## Problem 1: 2-Layer Neural Network for Regression~~

Write code that creates a 2-layer network (input→linear→relu→linear) that maps a 5-dimensional input to a 1-dimensional output and trains it on 100 random points.

- Use `torch.nn.Linear` and `torch.nn.functional.relu`
- Train the network for 10 epochs with SGD optimizer and print the loss after each epoch
- After training, test the model on a new input tensor `[[1,0,0,0,0]]` and print the result

---

## Problem 2: Multi-Class Classification with Label Smoothing

Demonstrate how to use `torch.nn.functional.cross_entropy` with logits and how to apply label smoothing.

- Create a 3-class classification problem with 20 random samples
- Train a linear layer to predict the class logits using `torch.nn.Linear(10,3)` and `torch.nn.functional.cross_entropy`
- Compute and print the accuracy of the model on the training data by comparing `torch.argmax(logits, dim=1)` with true labels
- Add label smoothing (e.g., 0.1) to the loss and retrain for 5 epochs, then report the new loss value
- Use the weight parameter in `cross_entropy` or `nn.BCEWithLogitsLoss` with smoothing

---

## Problem 3: Spatial Transformation with Grid Sample

Show how to use `torch.nn.functional.grid_sample` to perform a simple spatial transformation on a 1-channel image.

- Create a 1×8×8 tensor of zeros with a channel dimension
- Apply `grid_sample` with a mild random displacement field using `nn.functional.grid_sample(input, grid, padding_mode='zeros')`
- Print the shape of the output tensor and verify that it matches the input shape
- Modify the grid to shift the image 2 pixels to the right and down, then visualize or print the coordinates used for sampling
- Build a meshgrid and add offsets before passing to `grid_sample`

---

## Problem 4: Linear Regression with PyTorch

Learn to build and train a simple linear regression model using PyTorch's tensor operations and SGD optimizer.

- Create a synthetic dataset of x values from 0 to 9 and corresponding y = 2x + 1 values
- Train a linear model to fit this data for 100 epochs using mean squared error loss and stochastic gradient descent
- Print the loss every 10 epochs and the final learned weight and bias

---

## Problem 5: Gradient Descent Implementation from Scratch

Implement gradient descent from scratch to minimize a simple cost function.

- Write a function that minimizes the cost function J(θ) = θ²
- Start with θ = 5.0 and update θ using θ = θ - 0.1 × gradient
- Run for 10 iterations, printing θ at each step and the final value

---

## Problem 6: Tensor Operations and Shape Manipulation

Practice reshaping and transforming tensors using `view`, `reshape`, `unsqueeze`, and `squeeze` operations.

- Create a tensor of shape (3, 1, 4, 1)
- Use `squeeze` to remove all singleton dimensions and print the resulting shape
- Then `unsqueeze` at dimension 0 and print the new shape
