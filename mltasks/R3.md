| domain      | topic                  | subtopic                    | problem to solve                                                                                           | reasoning                                                                                                           |                                    |     |                                                                               |
| ----------- | ---------------------- | --------------------------- | ---------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------- | ---------------------------------- | --- | ----------------------------------------------------------------------------- |
| **CUDA**    | Kernel Optimization    | Shared Memory Tiling        | Write a tiled 2D shared-memory matrix multiplication kernel for 1024×1024 matrices and verify against CPU. | Tiling is the canonical technique to overcome memory-bandwidth limits in GEMM, the kernel behind all deep learning. |                                    |     |                                                                               |
| **CUDA**    | Kernel Optimization    | 2D Convolution              | Write a naive 2D convolution kernel (1 channel, 3×3 kernel) and test on a 32×32 image.                     | Directly maps to CNN operations; prerequisite for understanding cuDNN.                                              |                                    |     |                                                                               |
| **CUDA**    | Kernel Optimization    | Fused Ops                   | Write a single kernel that computes `C = ReLU(A + B)` element-wise.                                        | Fusion eliminates round-trips to global memory and kernel-launch overhead, a core GPU optimization.                 |                                    |     |                                                                               |
| **CUDA**    | Kernel Optimization    | Warp Reductions             | Implement a warp-parallel sum using `__shfl_down_sync` (sync variant) and verify against CPU sum.          | Modern CUDA requires explicit sync warp primitives; essential for safe intra-warp collectives.                      |                                    |     |                                                                               |
| **CUDA**    | Kernel Optimization    | Block Scan                  | Implement an inclusive prefix sum (scan) on a 1D array using only shared memory within a block.            | Scan is the building block for sparse operations, stream compaction, and advanced parallel algorithms.              |                                    |     |                                                                               |
| **CUDA**    | Kernel Optimization    | Bank Conflicts              | Write a shared-memory transpose kernel and fix bank conflicts by adding padding.                           | Shared-memory bank conflicts silently halve effective bandwidth; recognizing them is critical.                      |                                    |     |                                                                               |
| **CUDA**    | Libraries              | cuBLAS GEMM                 | Use `cublasSgemm` to multiply two 1024×1024 matrices; print the speedup vs a naive CUDA kernel.            | cuBLAS is the production standard; you must know when to drop custom kernels and call it.                           |                                    |     |                                                                               |
| **CUDA**    | Libraries              | cuBLAS Batched              | Use `cublasSgemmBatched` to multiply 100 pairs of 32×32 matrices.                                          | Batched GEMM appears in attention heads and multi-sample training loops.                                            |                                    |     |                                                                               |
| **CUDA**    | Libraries              | cuFFT 1D                    | Use cuFFT to compute the FFT of a 1D signal and verify against `numpy.fft.fft`.                            | FFT is foundational for signal/audio models and some efficient attention approximations.                            |                                    |     |                                                                               |
| **CUDA**    | Libraries              | cuFFT 2D                    | Use cuFFT to compute the 2D FFT of a 16×16 image, then reconstruct it with the inverse FFT.                | 2D FFT underlies frequency-domain image processing and certain generative techniques.                               |                                    |     |                                                                               |
| **CUDA**    | Memory & Concurrency   | Streams & Events            | Launch 3 kernels on 3 streams with async H2D copies; use CUDA events to measure overlap.                   | Hiding transfer behind compute is essential for pipeline efficiency in training and inference.                      |                                    |     |                                                                               |
| **CUDA**    | Memory & Concurrency   | Pinned Memory               | Allocate pinned host memory, perform async copies, and compare bandwidth vs pageable memory.               | Pinned memory is mandatory for true asynchronous transfers and maximum PCIe throughput.                             |                                    |     |                                                                               |
| **CUDA**    | Memory & Concurrency   | CUDA Graphs                 | Capture and replay a CUDA graph for a fixed sequence of 5 kernel launches.                                 | Graphs eliminate CPU launch overhead, critical for small-batch or repeated inference.                               |                                    |     |                                                                               |
| **CUDA**    | Advanced Features      | Constant Memory             | Store a 5×5 filter in `__constant__` memory and read it from a convolution kernel.                         | Constant memory is cached and optimal for uniformly accessed read-only data like filters.                           |                                    |     |                                                                               |
| **CUDA**    | Advanced Features      | Texture Memory              | Bind a 2D array to a texture object and perform bilinear sampling in a kernel.                             | Texture hardware is useful for image sampling and geometric deep learning tasks.                                    |                                    |     |                                                                               |
| **CUDA**    | Advanced Features      | Cooperative Groups          | Use cooperative groups to perform a grid-wide reduction with a single device-wide sync.                    | Needed for algorithms requiring global synchronization without returning to the host.                               |                                    |     |                                                                               |
| **CUDA**    | Advanced Features      | Dynamic Parallelism         | Launch a child kernel from within a parent kernel to process sub-blocks adaptively.                        | Useful for recursive algorithms and adaptive mesh refinement in scientific ML.                                      |                                    |     |                                                                               |
| **CUDA**    | Integration            | PyTorch C++ Extension       | Write a custom CUDA kernel for `x^2`, wrap it with `torch.utils.cpp_extension`, and call from Python.      | Bridges PyTorch flexibility with raw CUDA performance for research-grade custom ops.                                |                                    |     |                                                                               |
| **CUDA**    | Integration            | Triton Kernel               | Write a Triton kernel for vector addition and compare ease-of-use versus raw CUDA.                         | Triton is the emerging standard for writing fast GPU kernels in Pythonic ML research.                               |                                    |     |                                                                               |
| **CUDA**    | Profiling              | Nsight Compute              | Profile a simple kernel with Nsight Compute and report achieved occupancy and memory throughput.           | Optimization requires measurement; Nsight is the industry-standard profiling tool.                                  |                                    |     |                                                                               |
| **CUDA**    | Multi-GPU              | Peer Access                 | Enable peer access between 2 GPUs and perform a P2P memory copy, timing the transfer.                      | Foundation for model parallelism and large-scale distributed training.                                              |                                    |     |                                                                               |
| **CUDA**    | Tensor Cores           | WMMA                        | Use the WMMA API to perform a 16×16×16 mixed-precision (FP16) matrix multiply.                             | Tensor Cores are the hardware engine behind the 2–3× speedups in modern DL training.                                |                                    |     |                                                                               |
| **PyTorch** | Core Mechanisms        | Custom Autograd             | Implement a custom `autograd.Function` for `f(x)=x^3` with a manually defined backward pass.               | Understanding the autograd engine is necessary for any non-standard research operation.                             |                                    |     |                                                                               |
| **PyTorch** | Core Mechanisms        | Gradient Checkpointing      | Train a 10-layer MLP with gradient checkpointing every other layer; verify memory reduction.               | Essential for training very deep models when VRAM is the bottleneck.                                                |                                    |     |                                                                               |
| **PyTorch** | Core Mechanisms        | Mixed Precision             | Train a small CNN for 1 epoch using `torch.cuda.amp.autocast` and `GradScaler`.                            | Mixed precision is standard in frontier training for significant speedup without accuracy loss.                     |                                    |     |                                                                               |
| **PyTorch** | Building Blocks        | Dropout                     | Implement dropout from scratch (training mask, eval identity) in a `nn.Module` and verify gradients.       | Understanding the mask mechanics prevents misuse and aids debugging of stochastic layers.                           |                                    |     |                                                                               |
| **PyTorch** | Building Blocks        | BatchNorm1d                 | Implement batch normalization from scratch (running mean/var, train/eval mode) and match `nn.BatchNorm1d`. | BatchNorm is ubiquitous; manual implementation reveals the moving-average trick.                                    |                                    |     |                                                                               |
| **PyTorch** | Building Blocks        | LayerNorm                   | Implement layer normalization from scratch and verify against `nn.LayerNorm` on random data.               | LayerNorm is critical for transformers; must know why it avoids batch-size dependence.                              |                                    |     |                                                                               |
| **PyTorch** | Building Blocks        | GroupNorm                   | Implement group normalization from scratch (4 groups) and verify against `nn.GroupNorm`.                   | Used in modern vision models (ConvNeXt) where small batch sizes break BatchNorm.                                    |                                    |     |                                                                               |
| **PyTorch** | Building Blocks        | InstanceNorm                | Implement instance normalization from scratch for a 4D image tensor.                                       | Key for style transfer and certain generative architectures.                                                        |                                    |     |                                                                               |
| **PyTorch** | Building Blocks        | RMSNorm                     | Implement RMSNorm (root-mean-square normalization) from scratch and compare to LayerNorm.                  | RMSNorm replaces LayerNorm in LLaMA and most modern LLMs for training stability.                                    |                                    |     |                                                                               |
| **PyTorch** | Building Blocks        | GELU                        | Implement the GELU activation using its exact and approximate formulas; plot vs ReLU.                      | GELU is the standard activation in BERT, GPT, and vision transformers.                                              |                                    |     |                                                                               |
| **PyTorch** | Building Blocks        | SwiGLU                      | Write a `nn.Module` for the SwiGLU activation and show it is a gated linear unit.                          | SwiGLU is used in PaLM and LLaMA as the FFN activation.                                                             |                                    |     |                                                                               |
| **PyTorch** | Optimization           | SGD Momentum                | Implement SGD with momentum from scratch and train a linear model; compare convergence to vanilla SGD.     | Momentum is fundamental; implementing it reveals the velocity accumulator mechanism.                                |                                    |     |                                                                               |
| **PyTorch** | Optimization           | Adam                        | Implement Adam from scratch (with bias correction) and verify parameter updates match `torch.optim.Adam`.  | Adam dominates frontier ML; must understand first and second moment estimates.                                      |                                    |     |                                                                               |
| **PyTorch** | Optimization           | LR Schedulers               | Train a model with `CosineAnnealingLR` and compare the loss curve to `StepLR` on the same task.            | Scheduling is crucial for reaching state-of-the-art convergence.                                                    |                                    |     |                                                                               |
| **PyTorch** | Optimization           | Weight Decay                | Manually apply L2 weight decay in the parameter update and compare to `optimizer` `weight_decay`.          | Distinguishing decay from gradients is important for AdamW correctness.                                             |                                    |     |                                                                               |
| **PyTorch** | Optimization           | Gradient Clipping           | Implement gradient clipping by value and by norm; observe the effect on RNN training stability.            | Prevents exploding gradients in recurrent and very deep architectures.                                              |                                    |     |                                                                               |
| **PyTorch** | Optimization           | EMA                         | Maintain an exponential moving average of model weights during training and evaluate both sets.            | EMA is standard for GANs and diffusion models to stabilize sampling.                                                |                                    |     |                                                                               |
| **PyTorch** | Architectures          | Positional Encoding         | Implement sinusoidal positional encoding and add it to a batch of token embeddings.                        | Transformers have no recurrence; positional encoding provides sequence order.                                       |                                    |     |                                                                               |
| **PyTorch** | Architectures          | RoPE                        | Implement Rotary Positional Embeddings for a single head and apply to Q and K matrices.                    | RoPE is the dominant position encoding in LLaMA, Mistral, and modern GPTs.                                          |                                    |     |                                                                               |
| **PyTorch** | Architectures          | Causal Mask                 | Create a causal (look-ahead) mask for sequence length 10 and apply it to an attention score matrix.        | Autoregressive models (GPT) require masking future positions to prevent data leakage.                               |                                    |     |                                                                               |
| **PyTorch** | Architectures          | Multi-Head Attention        | Implement multi-head self-attention from scratch (no `nn.MultiheadAttention`) using reshape/einsum.        | Core of the transformer; must build once to debug ever.                                                             |                                    |     |                                                                               |
| **PyTorch** | Architectures          | GQA                         | Modify multi-head attention to Grouped Query Attention (2 query heads share 1 key/value head).             | GQA reduces memory bandwidth during inference in production LLMs.                                                   |                                    |     |                                                                               |
| **PyTorch** | Architectures          | Transformer Encoder         | Stack multi-head attention + feedforward + norm into a single `TransformerEncoderLayer`.                   | Building block of BERT and vision transformers.                                                                     |                                    |     |                                                                               |
| **PyTorch** | Architectures          | Transformer Decoder         | Implement a `TransformerDecoderLayer` with masked self-attention and cross-attention.                      | Needed for seq2seq (translation, original Transformer).                                                             |                                    |     |                                                                               |
| **PyTorch** | Architectures          | Full Transformer            | Train a tiny Transformer (2 layers, d\_model=64) on a synthetic copy-task dataset.                         | End-to-end integration of encoder-decoder with cross-attention.                                                     |                                    |     |                                                                               |
| **PyTorch** | Architectures          | BERT-style MLM              | Create a synthetic masked language modeling task: mask 15% of tokens and train to predict them.            | Understanding masked language modeling pretraining.                                                                 |                                    |     |                                                                               |
| **PyTorch** | Architectures          | GPT-style CLM               | Train a tiny GPT (4 layers, causal mask) on a synthetic next-token prediction task.                        | Causal language modeling is the foundation of modern LLMs.                                                          |                                    |     |                                                                               |
| **PyTorch** | Architectures          | ResNet Block                | Implement a Bottleneck ResNet block with skip connection and verify gradient flow through the shortcut.    | Residual connections enable training networks with 100+ layers.                                                     |                                    |     |                                                                               |
| **PyTorch** | Inference              | KV Cache                    | Implement KV cache for autoregressive generation and show it avoids redundant computation.                 | Essential for efficient transformer inference; without it, generation is O(n²) per step.                            |                                    |     |                                                                               |
| **PyTorch** | Inference              | Speculative Decoding        | Implement a toy speculative decoding loop: draft model generates 4 tokens, target model verifies them.     | Speculative decoding gives 2–3× inference speedup in modern serving systems.                                        |                                    |     |                                                                               |
| **PyTorch** | Generative Models      | VAE Encoder/Decoder         | Build a VAE for MNIST where the encoder outputs `mu`/`logvar` and the decoder reconstructs the image.      | Latent variable models are foundational for generative AI.                                                          |                                    |     |                                                                               |
| **PyTorch** | Generative Models      | VAE Reparameterization      | Implement the reparameterization trick manually and verify gradients flow to `mu` and `logvar`.            | Without this trick, VAE backpropagation is blocked through the sampling node.                                       |                                    |     |                                                                               |
| **PyTorch** | Generative Models      | VAE ELBO                    | Compute the VAE loss as reconstruction + KL divergence manually and compare to standard implementations.   | ELBO is the variational objective; must understand the two-term structure.                                          |                                    |     |                                                                               |
| **PyTorch** | Generative Models      | GAN Loss                    | Implement vanilla GAN loss (binary cross-entropy) for generator and discriminator on 1D data.              | Minimax game is the core concept behind adversarial training.                                                       |                                    |     |                                                                               |
| **PyTorch** | Generative Models      | Diffusion Forward           | Implement the forward diffusion process: add Gaussian noise to an image following a linear beta schedule.  | Diffusion models are state-of-the-art generative models.                                                            |                                    |     |                                                                               |
| **PyTorch** | Generative Models      | Diffusion Reverse Step      | Implement one reverse DDPM sampling step and denoise a pure noise image over T=10 steps.                   | Understanding the denoising process is key to diffusion inference.                                                  |                                    |     |                                                                               |
| **PyTorch** | Generative Models      | DDIM Sampling               | Implement a single DDIM sampling step and compare speed/quality vs DDPM on the same model.                 | DDIM enables 10–50× fewer sampling steps, crucial for practical deployment.                                         |                                    |     |                                                                               |
| **PyTorch** | Efficiency             | torch.compile               | Apply `torch.compile` to a small CNN and measure inference speedup on random inputs.                       | `torch.compile` is PyTorch 2.0's key performance feature.                                                           |                                    |     |                                                                               |
| **PyTorch** | Efficiency             | torch.fx                    | Use `torch.fx.symbolic_trace` to inspect the graph of a simple `nn.Module`.                                | FX transforms are used for quantization and optimization passes.                                                    |                                    |     |                                                                               |
| **PyTorch** | Efficiency             | Quantization (PTQ)          | Apply dynamic post-training quantization to a small MLP and compare model size and latency.                | Quantization reduces model size and speeds up inference on edge devices.                                            |                                    |     |                                                                               |
| **PyTorch** | Efficiency             | LoRA                        | Implement LoRA by freezing base weights and training low-rank A/B matrices for a linear layer.             | LoRA enables efficient fine-tuning of billion-parameter models.                                                     |                                    |     |                                                                               |
| **PyTorch** | Efficiency             | Knowledge Distillation      | Train a small "student" MLP to match a larger "teacher" MLP using soft targets (temperature).              | Distillation compresses frontier models for deployment.                                                             |                                    |     |                                                                               |
| **PyTorch** | Advanced Training      | Distributed DDP             | Set up a minimal `DistributedDataParallel` example on 2 processes for a linear model.                      | Multi-GPU training is required for frontier-scale experiments.                                                      |                                    |     |                                                                               |
| **PyTorch** | Advanced Training      | Gradient Accumulation       | Simulate batch size 64 by accumulating gradients over 4 steps of batch size 16.                            | Allows large effective batch sizes on limited memory.                                                               |                                    |     |                                                                               |
| **PyTorch** | Data & Preprocessing   | BPE Tokenizer               | Implement the byte-pair encoding merge loop on a tiny character corpus (e.g., 50 words).                   | Tokenization is the first step of all modern NLP pipelines.                                                         |                                    |     |                                                                               |
| **PyTorch** | Data & Preprocessing   | Embedding Bag               | Use `nn.EmbeddingBag` to compute mean embeddings for variable-length sequences without padding.            | Efficient for recommendation systems and text classification.                                                       |                                    |     |                                                                               |
| **NumPy**   | Advanced Indexing      | Strides                     | Use `np.lib.stride_tricks.as_strided` to create a 2D sliding-window view of a 1D array.                    | Strides underpin tensor views in PyTorch and efficient local operations.                                            |                                    |     |                                                                               |
| **NumPy**   | Advanced Indexing      | Einsum GEMM                 | Reimplement batch matrix multiplication using `np.einsum` and verify against `@`.                          | Einsum is the clearest notation for tensor contractions (attention, bilinear ops).                                  |                                    |     |                                                                               |
| **NumPy**   | Advanced Indexing      | Einsum Attention            | Use `np.einsum` to compute `Q @ K^T` and the attention output for random Q, K, V arrays.                   | Attention is all einsum; mastering this removes shape confusion.                                                    |                                    |     |                                                                               |
| **NumPy**   | Memory                 | Layout Effects              | Create a (1000,1000) array in C-order and F-order; time column-wise sum for both.                          | Layout affects cache performance and interop with other languages.                                                  |                                    |     |                                                                               |
| **NumPy**   | Numerical Methods      | Polynomial Fit              | Fit a degree-5 polynomial to noisy data with `np.polyfit` and evaluate with `np.polyval`.                  | Curve fitting is fundamental for modeling and calibration.                                                          |                                    |     |                                                                               |
| **NumPy**   | Numerical Methods      | Numerical Integration       | Approximate the integral of sin(x) from 0 to π using `np.trapz` and compare to the exact value 2.0.        | Numerical methods bridge continuous math and discrete computation.                                                  |                                    |     |                                                                               |
| **NumPy**   | Numerical Methods      | FFT Convolution             | Implement 1D convolution via `np.fft.fft` and the convolution theorem; verify against `np.convolve`.       | FFT-based convolution is faster for large kernels and used in signal/audio ML.                                      |                                    |     |                                                                               |
| **NumPy**   | Statistics             | Monte Carlo Pi              | Estimate π by generating random points in a unit square and computing the ratio inside the quarter-circle. | Monte Carlo methods are essential for probabilistic ML and Bayesian inference.                                      |                                    |     |                                                                               |
| **NumPy**   | Statistics             | Bootstrap                   | Compute a 95% confidence interval for the mean of a dataset using bootstrap resampling.                    | Statistical rigor in ML requires understanding sampling variability.                                                |                                    |     |                                                                               |
| **NumPy**   | Linear Algebra         | PCA                         | Implement PCA from scratch using SVD on a 2D dataset and project it to 1D.                                 | Dimensionality reduction is ubiquitous in data preprocessing and visualization.                                     |                                    |     |                                                                               |
| **NumPy**   | Linear Algebra         | Covariance                  | Compute the covariance matrix of a dataset and verify it is symmetric positive semi-definite.              | Covariance structures appear in Gaussians, embeddings, and optimization.                                            |                                    |     |                                                                               |
| **NumPy**   | Sparse                 | CSR Matrix                  | Convert a dense matrix to `scipy.sparse.csr_matrix` and perform a matrix-vector product.                   | Large graphs and NLP vocabularies require sparse representations.                                                   |                                    |     |                                                                               |
| **Math**    | Probability            | Gaussian PDF                | Derive and code the PDF of a univariate Gaussian; plot it for μ=0, σ=1 and σ=2.                            | Gaussian distributions are the backbone of noise models, VAEs, and diffusion.                                       |                                    |     |                                                                               |
| **Math**    | Probability            | Multivariate Gaussian       | Write the PDF formula for a 2D Gaussian and evaluate it at a point; note the covariance matrix.            | Latent spaces in VAEs and diffusion are multivariate Gaussians.                                                     |                                    |     |                                                                               |
| **Math**    | Probability            | Bayes Theorem               | Solve a discrete Bayes problem: given P(Disease)=0.01, P(+                                                 | Disease)=0.99, P(+                                                                                                  | NoDisease)=0.05, compute P(Disease | +). | Bayesian reasoning underlies probabilistic ML and uncertainty quantification. |
| **Math**    | Probability            | MLE                         | Derive the MLE for the mean of a Gaussian given N samples.                                                 | MLE is the philosophical foundation of loss functions (cross-entropy = MLE).                                        |                                    |     |                                                                               |
| **Math**    | Probability            | MAP                         | Derive the MAP estimate for a Gaussian mean with a Gaussian prior (shrinkage).                             | MAP connects regularization (L2 = Gaussian prior) to Bayesian inference.                                            |                                    |     |                                                                               |
| **Math**    | Information Theory     | Entropy                     | Compute the Shannon entropy of \[0.2, 0.5, 0.3] in bits.                                                   | Entropy measures uncertainty and underlies decision trees and compression.                                          |                                    |     |                                                                               |
| **Math**    | Information Theory     | KL Divergence               | Compute KL(P‖Q) for two discrete distributions and show it is asymmetric.                                  | KL appears in VAEs, variational inference, and policy gradients.                                                    |                                    |     |                                                                               |
| **Math**    | Information Theory     | Cross-Entropy               | Show that minimizing cross-entropy is equivalent to minimizing KL divergence plus entropy.                 | Justifies why cross-entropy is the standard classification loss.                                                    |                                    |     |                                                                               |
| **Math**    | Calculus               | Chain Rule (Multivar)       | Given z=f(x,y), x=g(t), y=h(t), compute dz/dt using the multivariable chain rule for concrete polynomials. | Backpropagation is literally the chain rule applied to a computation graph.                                         |                                    |     |                                                                               |
| **Math**    | Linear Algebra         | QR Decomposition            | Compute the QR decomposition of a 3×3 matrix and verify Q is orthogonal.                                   | QR is used in stable least-squares and some orthogonal initialization schemes.                                      |                                    |     |                                                                               |
| **Math**    | Linear Algebra         | Cholesky                    | Compute the Cholesky decomposition of a symmetric positive-definite matrix and reconstruct it.             | Cholesky is used for efficiently sampling multivariate Gaussians.                                                   |                                    |     |                                                                               |
| **Math**    | Linear Algebra         | PSD                         | Prove that AᵀA is positive semi-definite for any real matrix A by showing xᵀ(AᵀA)x ≥ 0.                    | PSD matrices define valid covariance structures and kernel methods.                                                 |                                    |     |                                                                               |
| **Math**    | Linear Algebra         | Condition Number            | Compute the condition number of a matrix via SVD and explain its link to numerical stability.              | High condition number means gradients explode or vanish; critical for debugging training.                           |                                    |     |                                                                               |
| **Math**    | Linear Algebra         | Spectral Norm               | Compute the spectral norm of a matrix via SVD and relate it to the Lipschitz constant of the linear map.   | Spectral normalization stabilizes GAN training by constraining the discriminator.                                   |                                    |     |                                                                               |
| **Math**    | Optimization           | Convexity                   | Prove f(x)=x² is convex using the definition f(tx+(1-t)y) ≤ tf(x)+(1-t)f(y).                               | Convex optimization guarantees global minima; tools transfer to non-convex neural nets.                             |                                    |     |                                                                               |
| **Math**    | Optimization           | GD Convergence              | Show that gradient descent on f(x)=x² with step size α=0.1 converges geometrically.                        | Understanding convergence rates explains learning rate choices and instability.                                     |                                    |     |                                                                               |
| **Math**    | Optimization           | Momentum                    | Write the update equations for Polyak momentum and solve them analytically for a 1D quadratic.             | Momentum averages past gradients; solving analytically shows oscillation damping.                                   |                                    |     |                                                                               |
| **Math**    | Optimization           | Adam Derivation             | Write the Adam update rule with bias correction and explain why epsilon prevents division by zero.         | Adaptive optimizers are standard; must know what the moving averages represent.                                     |                                    |     |                                                                               |
| **Math**    | Deep Learning Math     | Backprop BatchNorm          | Derive the gradient of loss w\.r.t. input x for a BatchNorm layer (simplified 1D).                         | BatchNorm has a complex backward pass; deriving it prevents black-box confusion.                                    |                                    |     |                                                                               |
| **Math**    | Deep Learning Math     | Backprop LayerNorm          | Derive the gradient w\.r.t. input for LayerNorm and note the similarity/difference to BatchNorm.           | Transformers rely on LayerNorm; its gradient structure affects initialization.                                      |                                    |     |                                                                               |
| **Math**    | Deep Learning Math     | Attention Gradient          | Derive dL/dQ given dL/d(softmax(QKᵀ/√d)V) for a single head.                                               | Custom attention variants require knowing how gradients flow through softmax.                                       |                                    |     |                                                                               |
| **Math**    | Deep Learning Math     | ELBO                        | Write the ELBO for a VAE and identify the reconstruction term and KL term.                                 | Variational inference turns intractable integrals into an optimization objective.                                   |                                    |     |                                                                               |
| **Math**    | Deep Learning Math     | Reparameterization Gradient | Show why the gradient of E\[ε]\[f(μ+σε)] w\.r.t. μ is well-defined.                                        | The trick that makes VAE training possible via backprop through random nodes.                                       |                                    |     |                                                                               |
| **Math**    | Deep Learning Math     | Score Matching              | Define the score function for a Gaussian and show it equals the gradient of the log-PDF.                   | Score-based diffusion models rely on estimating this gradient.                                                      |                                    |     |                                                                               |
| **Math**    | Signal Processing      | Convolution Theorem         | State and verify numerically that conv(a,b) = IFFT(FFT(a)·FFT(b)) for 1D signals.                          | FFT-based methods speed up large-kernel convolutions and some attention variants.                                   |                                    |     |                                                                               |
| **Math**    | Signal Processing      | Nyquist Theorem             | Explain why sampling rate > 2×max\_frequency is needed and demonstrate aliasing with a sine wave.          | Critical for audio ML and any discretization of continuous signals.                                                 |                                    |     |                                                                               |
| **Math**    | Reinforcement Learning | Bellman Equation            | Write the Bellman equation for Q\*(s,a) and solve it iteratively for a 2-state, 2-action MDP.              | RL fundamentals underpin advanced training methods like RLHF.                                                       |                                    |     |                                                                               |
| **Math**    | Reinforcement Learning | Policy Gradient             | Derive the REINFORCE gradient estimator for a 1-step MDP with discrete actions.                            | Policy gradients are used in RLHF and autonomous agent training.                                                    |                                    |     |                                                                               |
| **Math**    | Learning Theory        | Bias-Variance               | Decompose the expected prediction error of an estimator into bias² + variance + noise.                     | Explains overfitting/underfitting and guides model capacity choices.                                                |                                    |     |                                                                               |
| **Math**    | Learning Theory        | Lipschitz                   | Show that the sigmoid function is Lipschitz continuous and find its Lipschitz constant.                    | Lipschitz constraints appear in WGAN and stability proofs for GANs/diffusion.                                       |                                    |     |                                                                               |
